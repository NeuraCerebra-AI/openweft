> **COMPACTION PROTOCOL**: After every compaction, the full ledger must be reread before any further work begins. The ledger is the source of truth for what has been completed, what remains, and what has changed. The next action must be chosen only after reviewing the ledger in full.

# Backend Hardening Rest Waves

## Task Statement

- Goal: complete the remaining backend-hardening waves after Wave 1, while preserving OpenWeft's simplicity, avoiding overengineering, and treating `prompts/prompt-a.md` as fixed and untouchable.
- Scope:
  - Wave 2: dry-run/mock semantic parity, merge-time re-analysis tightening, stale worktree/branch cleanup hardening
  - Wave 3: interruption/restart e2e coverage, orchestrator-level merge-conflict coverage, packaged CLI smoke test
  - Wave 4: minimal `realRun.ts` seam extraction and targeted dry-run/mock deduplication
- Hard constraints:
  - do not edit `prompts/prompt-a.md`
  - do not leak backend internals into the UI
  - prefer the smallest truthful patch over architectural cleverness
  - preserve Prompt B as the powerful worker brief and keep OpenWeft as the wrapper/orchestrator

## Research Snapshot

- Wave 1 is already integrated on `main` through commit `56a1a29`.
- Baseline in this execution worktree passed before any new edits:
  - `npm test`
  - result: `63` files, `615` tests passed
- Current high-risk implementation surfaces for the remaining waves:
  - `src/orchestrator/realRun.ts` at `2491` lines remains the main orchestration concentration point.
  - `src/orchestrator/dryRun.ts` at `566` lines still duplicates a meaningful subset of real planning/execution orchestration semantics.
  - `src/adapters/mock.ts` at `273` lines still encodes stage semantics and mock execution behavior separately from the real runtime.
- Existing runtime signals relevant to Wave 2:
  - `src/orchestrator/realRun.ts` already carries Prompt B persistence, reusable execution recovery, ledger enforcement, conflict-resolution retries, and re-analysis batching.
  - `src/git/worktrees.ts` already exposes `findReusableExecutionCommit(...)`, `getWorktreeStatusSummary(...)`, and merge helpers, but stale-registration handling remains a likely restart/recovery seam.
  - `src/domain/editSummary.ts` already captures rename metadata via `old_path`, so the re-analysis improvement should reuse existing git truth rather than inventing new semantic analysis.
- Existing test concentration:
  - `tests/orchestrator/realRun.test.ts` already contains many seam tests for recovery, Prompt B persistence, ledger enforcement, re-analysis, and planner/adjustment behavior.
  - `tests/e2e/cli-real-mock.test.ts` and `tests/e2e/cli-dry-run.test.ts` are the best public-beta harness truth surfaces for Wave 2 and Wave 3.
- Architectural north star reaffirmed:
  - Prompt A remains fixed and powerful.
  - Prompt B remains the execution brief.
  - OpenWeft should get more robust by becoming a better wrapper, not by becoming smarter than Prompt B.

## Non-Negotiable Guardrails

- Do not edit `prompts/prompt-a.md`.
- Keep Prompt B powerful and first-class internally.
- Keep worktree ownership with OpenWeft.
- Prefer git truth and checkpoint truth over narrative or inferred truth.
- Do not add new persistent schemas unless existing git/checkpoint/runtime truth is insufficient.
- Avoid broad refactors until correctness and harness truth are closed.
- When extracting code from `realRun.ts`, extract only obvious seams with stable contracts that already exist in practice.

## High-Level Approach Brainstorm

### Approach A: Execute Waves 2-4 exactly in queue order with surgical patches only

- Blast radius: Low-to-Medium
- Reversibility: High
- Dependency complexity: Low
- Implementation effort: Medium
- Long-term maintainability: Medium
- Score: `9/10`
- Notes:
  - Best fit for the repo's current state
  - Preserves correctness-first sequencing
  - Lets Wave 4 extraction happen only after the runtime contracts have been tightened

### Approach B: Skip ahead to `realRun.ts` decomposition first, then clean up correctness while extracting seams

- Blast radius: High
- Reversibility: Medium
- Dependency complexity: High
- Implementation effort: High
- Long-term maintainability: Medium
- Score: `3/10`
- Notes:
  - Tempting for cleanliness
  - Very likely to overengineer and destabilize the runtime while correctness gaps still exist

### Approach C: Treat Wave 2 and Wave 3 as test-only work, then defer all runtime hardening until Wave 4

- Blast radius: Medium
- Reversibility: High
- Dependency complexity: Medium
- Implementation effort: Low-to-Medium
- Long-term maintainability: Low
- Score: `5/10`
- Notes:
  - Improves confidence but leaves real runtime seams loose
  - Risks building confidence on top of still-brittle runtime behavior

### Approach D: Fold all remaining waves into one broad backend simplification pass

- Blast radius: Very High
- Reversibility: Low
- Dependency complexity: Very High
- Implementation effort: Very High
- Long-term maintainability: Unclear
- Score: `1/10`
- Notes:
  - Exactly the kind of overengineering drift this ledger is meant to prevent

### Approach E: Stop after Wave 3 and leave Wave 4 for later

- Blast radius: Low
- Reversibility: High
- Dependency complexity: Low
- Implementation effort: Medium
- Long-term maintainability: Medium-Low
- Score: `6/10`
- Notes:
  - Reasonable if only public-beta confidence mattered
  - Undershoots the user request to complete the remaining waves autonomously

## Selected High-Level Approach

- Chosen: **Approach A**
- Why:
  - lowest blast radius path that still completes the remaining queue honestly
  - preserves the "fix truth boundaries first, simplify last" ordering
  - best matches the requirement to avoid overengineering while still finishing the remaining waves
- Confidence:
  - `89%` that the safest route is still the ordered wave execution already encoded in the hardening queue
  - `93%` that `prompt-a.md` should remain untouched throughout this work

## Concrete Implementation Strategy Brainstorm

### Strategy 1: Execute each queue item as its own isolated commit slice, with targeted tests and downstream review before marking each major step complete

- Risk of cascading failures: Low
- Operational complexity: Medium
- Implementation clarity: High
- Observability and debuggability: High
- Compatibility with existing architecture: High
- Score: `10/10`
- Notes:
  - best balance of autonomy, reviewability, and recoverability
  - keeps each wave understandable if execution is interrupted

### Strategy 2: Implement an entire wave first, then add tests and review at the end of the wave

- Risk of cascading failures: Medium
- Operational complexity: Low
- Implementation clarity: Medium
- Observability and debuggability: Medium-Low
- Compatibility with existing architecture: Medium
- Score: `5/10`
- Notes:
  - faster in the short term
  - weaker at catching drift before it spreads

### Strategy 3: Use only subagents for implementation and keep the controller session mostly orchestration-only

- Risk of cascading failures: Medium
- Operational complexity: Medium-High
- Implementation clarity: Medium
- Observability and debuggability: Medium
- Compatibility with existing architecture: Medium
- Score: `6/10`
- Notes:
  - useful for independent research and reviews
  - too much delegation risks duplicated context and avoidable merge churn for tightly coupled runtime edits

### Strategy 4: Do all runtime changes first, then come back and retrofit dry-run/mock/tests to match

- Risk of cascading failures: High
- Operational complexity: Medium
- Implementation clarity: Low
- Observability and debuggability: Low
- Compatibility with existing architecture: Low
- Score: `2/10`
- Notes:
  - exactly how semantic drift sneaks in

### Strategy 5: Prioritize Wave 4 extraction before Wave 2 parity so later changes land in smaller files

- Risk of cascading failures: High
- Operational complexity: High
- Implementation clarity: Medium
- Observability and debuggability: Medium
- Compatibility with existing architecture: Medium-Low
- Score: `4/10`
- Notes:
  - appealing aesthetically
  - wrong order for minimizing blast radius

## Selected Implementation Strategy

- Chosen: **Strategy 1**
- Why:
  - fewest surprises
  - strongest fit for ledger-driven autonomous execution
  - easiest to pause/resume without losing the shape of the remaining work
- File-touch budget:
  - Wave 2: prefer changes to `src/orchestrator/dryRun.ts`, `src/adapters/mock.ts`, `src/orchestrator/realRun.ts`, `src/git/worktrees.ts`, `src/domain/editSummary.ts`, and the directly paired tests only
  - Wave 3: prefer test and release-gate files only unless a failing test proves a runtime change is required
  - Wave 4: keep seam extraction to one or two new helper modules plus the directly paired call sites; avoid broad moves/renames

## Ordered Major Steps

### Step W0

- **Step ID**: `W0`
- **Title**: `Ledger Setup And Remaining-Wave Research`
- **Objective**: Establish the canonical execution ledger, confirm the remaining queue, and ground the rest of the work in current repo reality.
- **Why This Step Exists**: The remaining waves are broad enough that execution needs a durable source of truth before code changes begin.
- **Dependencies**: None
- **Preconditions**:
  - clean isolated worktree exists
  - baseline verification completed in the worktree
- **Planned Actions**:
  - create this ledger
  - capture the remaining wave queue and guardrails
  - collect focused repo research for Wave 2, Wave 3, and Wave 4
- **Risk Level**: `Low`
- **Potential Blast Radius**: Planning/documentation only; `5%`
- **Rollback / Recovery Notes**: Delete the ledger and worktree if this setup proves mis-scoped.
- **Validation / Completion Criteria**:
  - ledger exists in `project_ledgers`
  - remaining-wave execution order is explicit
  - baseline test evidence is recorded
- **Affected Files / Systems**:
  - `project_ledgers/12.backend-hardening-rest-waves.md`
  - repo planning documents
- **Downstream Steps Potentially Impacted**:
  - `W1`
  - `W2`
  - `W3`
  - `W4`
- **Status**: `Complete`
- **Notes / Discoveries**:
  - Isolated worktree created at `.worktrees/backend-hardening-rest`.
  - Baseline `npm test` passed: `63` files, `615` tests.
  - Remaining-wave explorer agents have been dispatched for Wave 2, Wave 3, and Wave 4 reconnaissance.
  - Wave 3 and Wave 4 research have returned and been integrated into this ledger.
  - Wave 2 scout timed out once; local repo evidence is sufficient to begin the first parity slice without waiting.

### Step W1

- **Step ID**: `W1`
- **Title**: `Wave 2 Trust And Runtime-Parity Hardening`
- **Objective**: Make dry-run/mock semantics track the real runtime more truthfully, tighten merge-time re-analysis with existing rename/edit-summary truth, and harden stale worktree/branch cleanup paths.
- **Why This Step Exists**: Wave 2 is where backend trust improves materially without rewriting architecture.
- **Dependencies**:
  - `W0`
- **Preconditions**:
  - ledger setup complete
  - Wave 2 hot seams confirmed from repo research
- **Planned Actions**:
  - tighten dry-run/mock around Prompt B and stage semantics
  - tighten merge-time re-analysis using existing rename metadata and real merged-path truth
  - harden stale worktree/branch cleanup to prefer boring, idempotent recovery
  - add targeted tests for each runtime seam before or alongside the minimal implementation changes
  - run a downstream impact review with **2 verification agents** before marking the major step complete because this step is cross-cutting and runtime-affecting
- **Risk Level**: `Medium`
- **Potential Blast Radius**: Backend execution truth, restart behavior, and harness semantics; `35%`
- **Rollback / Recovery Notes**:
  - keep changes sliced by sub-step/commit so any bad seam can be reverted independently
  - if a cleanup or parity change proves noisy, revert to the last passing commit and narrow the helper instead of widening the abstraction
- **Validation / Completion Criteria**:
  - targeted Wave 2 suites pass
  - full `npm test` passes
  - verification agents confirm no unexpected contract drift into later waves
- **Affected Files / Systems**:
  - `src/orchestrator/dryRun.ts`
  - `src/adapters/mock.ts`
  - `src/orchestrator/realRun.ts`
  - `src/git/worktrees.ts`
  - `src/domain/editSummary.ts`
  - paired tests in `tests/orchestrator/`, `tests/e2e/`, and `tests/git/`
- **Downstream Steps Potentially Impacted**:
  - `W2`
  - `W3`
  - `W4`
- **Status**: `Complete`
- **Notes / Discoveries**:
  - Avoid semantic AI overlap logic; use existing git/edit-summary truth only.
  - First slice chosen: make dry-run use the real stage-two plan/manifest while keeping execution non-destructive via a scratch workspace instead of the repo root.
  - Second slice chosen: narrow re-analysis to only overlapping remaining features using actual merged-path truth, with rename support coming from `old_path`.
  - Final result:
    - dry-run now reflects actual stage-two manifests without mutating the repo root
    - re-analysis now skips unrelated remaining work instead of adjusting everything
    - stale-cleanup review did not justify extra churn; the first concrete stale-registration candidate was already handled by current recovery behavior
  - Downstream impact review outcome:
    - no evidence of pressure to touch UI, prompts, or checkpoint schema
    - leaving cleanup unchanged after the explicit stale-registration check was the simpler and more honest move
  - Avoid touching UI/status surfaces.

#### Sub-step W1.1

- **Step ID**: `W1.1`
- **Title**: `Dry-Run And Mock Semantic Parity`
- **Objective**: Make dry-run/mock prove the same Prompt-A -> Prompt-B -> plan/execution semantics that the real runtime now depends on.
- **Why This Step Exists**: Harnesses that only validate headings or file existence can hide real semantic drift.
- **Dependencies**:
  - `W1`
- **Preconditions**:
  - current real runtime contract reviewed in `realRun.ts`
- **Planned Actions**:
  - audit duplicated dry-run/mock planning/execution assumptions
  - tighten the smallest contract gaps first
  - strengthen e2e tests around Prompt B usage and stage semantics
- **Risk Level**: `Medium`
- **Potential Blast Radius**: Test harness truth and mock behavior; `20%`
- **Rollback / Recovery Notes**: Revert mock/dry-run deltas separately if the harness becomes noisier without improving semantic truth.
- **Validation / Completion Criteria**:
  - targeted dry-run/mock tests fail first where needed, then pass
  - no change to UI or prompt files
- **Affected Files / Systems**:
  - `src/orchestrator/dryRun.ts`
  - `src/adapters/mock.ts`
  - `tests/e2e/cli-real-mock.test.ts`
  - `tests/e2e/cli-dry-run.test.ts`
- **Downstream Steps Potentially Impacted**:
  - `W2`
  - `W3`
- **Status**: `Complete`
- **Notes / Discoveries**:
  - Keep support systems truthful, not exhaustive.
  - Current mismatch: `src/orchestrator/dryRun.ts` discards the stage-two plan, writes a synthetic empty-manifest plan, and therefore lies about manifest-driven phasing.
  - Constraint: fixing that by itself would currently make dry-run mutate the repo root, because mock execution applies the plan manifest to `cwd`.
  - Implemented:
    - dry-run now persists the actual stage-two plan markdown
    - dry-run now parses and stores the actual manifest
    - dry-run executes mock work in `.openweft/dry-run-workspaces/<featureId>` scratch space instead of the repo root
    - e2e dry-run coverage now asserts both manifest truth and repo non-mutation

#### Sub-step W1.2

- **Step ID**: `W1.2`
- **Title**: `Merge-Time Re-Analysis Tightening`
- **Objective**: Ensure remaining features are re-adjusted based on actual merged-path truth, including renames, without inventing new semantic machinery.
- **Why This Step Exists**: Remaining plans can become stale after merges if rename metadata or actual changed-path truth is ignored.
- **Dependencies**:
  - `W1`
  - `W1.1` should land first if test harness changes affect re-analysis tests
- **Preconditions**:
  - current re-analysis batching and edit-summary behavior inspected
- **Planned Actions**:
  - reuse `old_path` and actual merged-path sets consistently
  - keep overlap logic deterministic and path-based
  - tighten only the narrow re-analysis rule
- **Risk Level**: `Medium`
- **Potential Blast Radius**: Post-merge plan adjustment timing and correctness; `18%`
- **Rollback / Recovery Notes**: If a rename edge causes noisy false positives, revert to the previous overlap rule and re-land with a narrower matcher.
- **Validation / Completion Criteria**:
  - targeted re-analysis tests pass for rename and overlap cases
  - no unrelated phasing/scheduling behavior changes
- **Affected Files / Systems**:
  - `src/orchestrator/realRun.ts`
  - `src/domain/editSummary.ts`
  - `tests/orchestrator/realRun.test.ts`
- **Downstream Steps Potentially Impacted**:
  - `W2`
  - `W4`
- **Status**: `Complete`
- **Notes / Discoveries**:
  - Existing `EditSummary` already models `old_path`, so this should be a use-the-data-better step, not a new data-model step.
  - Implemented:
    - added a small `listEditSummaryPaths(...)` helper that returns both current and old rename paths
    - re-analysis now skips unrelated remaining features instead of adjusting everything
    - orchestrator coverage now proves unrelated remaining work is left alone

#### Sub-step W1.3

- **Step ID**: `W1.3`
- **Title**: `Stale Worktree And Branch Cleanup Hardening`
- **Objective**: Make restart and cleanup paths more idempotent when worktree directories or git registrations drift out of sync.
- **Why This Step Exists**: Recovery gets weird and fragile when stale branch refs or missing worktree directories block clean reruns.
- **Dependencies**:
  - `W1`
- **Preconditions**:
  - restart/recovery probes reviewed for fail-closed behavior
- **Planned Actions**:
  - wrap remaining git probes defensively
  - make stale cleanup more forceful where it is safe
  - prefer clean rerun over clever reuse when git state is ambiguous
- **Risk Level**: `Medium`
- **Potential Blast Radius**: Restart and cleanup behavior; `22%`
- **Rollback / Recovery Notes**: If cleanup proves too aggressive, revert to the prior narrower deletion path and re-land with extra guard checks.
- **Validation / Completion Criteria**:
  - targeted restart/cleanup tests pass
  - rerun paths remain safe and deterministic
- **Affected Files / Systems**:
  - `src/orchestrator/realRun.ts`
  - `src/git/worktrees.ts`
  - `tests/orchestrator/realRun.test.ts`
  - `tests/git/worktrees.test.ts`
- **Downstream Steps Potentially Impacted**:
  - `W2`
  - `W3`
- **Status**: `Complete`
- **Notes / Discoveries**:
  - Keep git truth primary.
  - First stale-registration candidate was tested explicitly: a checkpoint path missing on disk while git still had the registration. Current runtime already handled that case cleanly, so no code change was forced there.
  - Decision:
    - do not add cleanup code just to satisfy the checklist
    - preserve the simpler current behavior because the first concrete failure shape was already covered

### Step W2

- **Step ID**: `W2`
- **Title**: `Wave 3 Public-Beta Hardening`
- **Objective**: Add the missing high-value interruption/restart, merge-conflict, and packaged-CLI release coverage that public publication depends on.
- **Why This Step Exists**: The backend can be good and still feel shaky publicly if ugly-path coverage is missing.
- **Dependencies**:
  - `W1`
- **Preconditions**:
  - Wave 2 runtime/harness semantics stable
- **Planned Actions**:
  - add deterministic interruption/restart flows covering mid-planning and mid-execution
  - add orchestrator-level merge-conflict path coverage
  - add a packed-artifact CLI smoke test into the release gate
  - run a downstream impact review with **2 verification agents** before marking the major step complete because release-gate and restart semantics are cross-cutting
- **Risk Level**: `Medium`
- **Potential Blast Radius**: Test confidence, release confidence, restart guarantees; `28%`
- **Rollback / Recovery Notes**:
  - coverage-only failures should be fixed by tightening tests or the narrow runtime seam they expose, not by weakening the expectations
  - if the release gate becomes flaky, keep the smoke test local to CI/release checks rather than broadening runtime logic
- **Validation / Completion Criteria**:
  - targeted Wave 3 suites pass
  - `npm run release:check` passes
  - reviewers confirm no hidden public-beta blockers surfaced by the new coverage
- **Affected Files / Systems**:
  - `tests/orchestrator/realRun.test.ts`
  - `tests/e2e/`
  - `tests/release/`
  - `package.json`
  - `.github/workflows/ci.yml`
- **Downstream Steps Potentially Impacted**:
  - `W3`
  - `W4`
- **Status**: `Complete`
- **Notes / Discoveries**:
  - Prefer one or two high-value interruption flows, not exhaustive crash simulation.
  - Final result:
    - packaged CLI smoke gate added and verified inside `release:check`
    - orchestrator-level merge-conflict resolution path now has direct coverage
    - restart/interruption coverage was reviewed against the current suite and earlier restart hardening work; the repo already had the highest-value planning/execution interruption cases covered, so no extra crash-simulation machinery was added
  - Downstream impact review outcome:
    - this wave improved public-beta confidence without changing runtime contracts

### Step W3

- **Step ID**: `W3`
- **Title**: `Wave 4 Minimal Simplification`
- **Objective**: Simplify the backend only where correctness and test truth have already made the seam obvious, starting with the least ambiguous `realRun.ts` extraction and then narrowing duplicated dry-run/mock contract logic.
- **Why This Step Exists**: The repo is close to public-beta shape, but `realRun.ts` remains a maintenance trap; simplification should happen last and only where it buys clarity without ceremony.
- **Dependencies**:
  - `W2`
- **Preconditions**:
  - earlier waves green and stable
  - candidate extraction seam chosen based on actual runtime pain, not aesthetics
- **Planned Actions**:
  - pick one low-risk seam from `realRun.ts` for extraction
  - preserve behavior exactly while shrinking the god file slightly
  - use the extracted seam to remove only the most obvious duplicated contract logic in dry-run/mock
  - run a downstream impact review with **2 verification agents** before marking the major step complete because this step changes shared architecture and file boundaries
- **Risk Level**: `Medium`
- **Potential Blast Radius**: Shared orchestration behavior and maintainability surface; `30%`
- **Rollback / Recovery Notes**:
  - extract one seam at a time
  - if the seam starts forcing unrelated renames or abstractions, stop and revert to the last passing commit
- **Validation / Completion Criteria**:
  - file extraction preserves all existing behavior
  - targeted tests plus full `npm test` remain green
  - reviewers agree the simplification reduced complexity rather than redistributing it
- **Affected Files / Systems**:
  - `src/orchestrator/realRun.ts`
  - one or two new orchestrator helper modules
  - possibly `src/orchestrator/dryRun.ts`
  - possibly `src/adapters/mock.ts`
  - paired tests
- **Downstream Steps Potentially Impacted**:
  - `W4`
- **Status**: `Complete`
- **Notes / Discoveries**:
  - This is the step most likely to drift into overengineering if not constrained.
  - Final result:
    - extracted the planning/document-repair seam from `realRun.ts` into `src/orchestrator/planMarkdown.ts`
    - reduced dry-run/mock duplication by deleting the synthetic dry-run plan builder and using the real stage-two plan instead
  - Guardrail check:
    - no prompt changes
    - no new orchestration framework
    - no fake shared state machine between real-run and dry-run/mock

### Step W4

- **Step ID**: `W4`
- **Title**: `Final Verification, Ledger Closure, And Integration`
- **Objective**: Verify the completed remaining waves as one coherent backend hardening set, update the ledger to reflect final reality, and integrate cleanly.
- **Why This Step Exists**: The remaining waves should land as a verified whole, not just as individually passing slices.
- **Dependencies**:
  - `W3`
- **Preconditions**:
  - Waves 2-4 implemented and individually validated
- **Planned Actions**:
  - run final typecheck/test/release-gate verification
  - request one final whole-change review pass
  - update the ledger with final discoveries, residual risks, and actual landed sequencing
  - commit and integrate cleanly
- **Risk Level**: `Low`
- **Potential Blast Radius**: Verification and integration only; `10%`
- **Rollback / Recovery Notes**: Revert the final tranche commit(s) if integration verification fails.
- **Validation / Completion Criteria**:
  - `npm run typecheck` passes
  - `npm test` passes
  - `npm run release:check` passes
  - final reviewer has no major findings
- **Affected Files / Systems**:
  - all files touched by Waves 2-4
  - this ledger
- **Downstream Steps Potentially Impacted**:
  - none
- **Status**: `Complete`
- **Notes / Discoveries**:
  - Final state should be robust public-beta backend shape without touching Prompt A.
  - Final verification now passed on the remaining-wave slice:
    - `npm run typecheck`
    - `npm test`
    - `npm run release:check`
  - Latest results:
    - `63` test files, `618` tests passed
    - packaged CLI smoke succeeded inside the release gate
  - Integrated to `main` as commit `d055f21`.
  - Temporary `backend-hardening-rest` worktree and branch were removed after integration.
  - Prompt A remained untouched throughout the remaining-wave execution.

## Current Status Snapshot

- `W0`, `W1`, `W2`, `W3`, and `W4` are complete.
- Remaining-wave backend hardening is integrated on `main` as `d055f21`.
- Final repo state after integration:
  - `main` is clean
  - the temporary execution worktree is removed
  - Prompt A remained untouched

## Decisions Log

- `2026-03-20`: Locked the spiritual north star into the ledger:
  - do not overengineer
  - do not edit `prompts/prompt-a.md`
  - strengthen the wrapper/orchestrator, not the prompt core
- `2026-03-20`: Chose ordered wave execution over early refactor because correctness and harness truth still outrank code-beauty work.
- `2026-03-20`: Wave 3 scout confirmed the safest order remains `restart coverage -> merge-conflict coverage -> packaged CLI smoke`, with most work kept in `tests/orchestrator/realRun.test.ts`, `tests/git/worktrees.test.ts`, and release-gate files rather than broad runtime edits.
- `2026-03-20`: Wave 4 scout confirmed the least risky simplification seam is planning/document repair first; do not lead with broad `realRun.ts` lifecycle extraction or force dry-run/mock into a fake shared orchestration core.
- `2026-03-20`: Wave 2 scout recommended the safest internal order as `stale cleanup -> merge-time re-analysis -> dry-run/mock parity`, because parity should mirror finalized real-path semantics rather than lead them. I am keeping the current dry-run parity slice because it is already small, green, and self-contained, but I will treat the rest of Wave 2 in that scout-recommended order.
- `2026-03-20`: Chosen first Wave 2 implementation slice:
  - use the actual stage-two plan/manifest in dry-run planning
  - keep dry-run non-destructive by executing the mock plan in a scratch workspace rather than the repo root
  - strengthen dry-run e2e assertions around manifest truth and non-mutation
- `2026-03-20`: Chosen second Wave 2 implementation slice:
  - use actual merged-path overlap to decide which remaining features need adjustment
  - count both `path` and `old_path` for rename-aware overlap
  - update stale tests that were only proving the old “adjust everything” behavior
- `2026-03-20`: Wave 3 item 7 closed by review, not new machinery:
  - the repo already had the highest-value interruption/restart cases covered after prior hardening
  - adding more crash simulation here would have risked ceremony over value
- `2026-03-20`: Wave 4 item 11 partially resolved as a side effect of better truth:
  - removing the synthetic dry-run plan builder was the right deduplication
  - no broader dry-run/mock abstraction was justified

## Next Action

- Commit this finalized ledger artifact on `main`, then push the already-landed backend-hardening changes.
